import React from 'react';
import Title from '../../../useTitle';

const Home = () => {
    Title('home')
    return (
        <div>
            <h2>this is home</h2>
        </div>
    );
};

export default Home;